/**
 * En web las actualizaciones las gestiona el service worker (sw.js): al recargar
 * la página se sirve la versión nueva. No hace falta banner de EAS Update.
 */
export function UpdateBanner() {
  return null;
}
